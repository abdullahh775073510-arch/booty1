<!DOCTYPE html>
<html>
<head>
    <title>🎮 لودو ستار</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        body {
            margin: 0;
            padding: 20px;
            background: purple;
            color: white;
            text-align: center;
            font-family: Arial;
        }
        .btn {
            background: red;
            color: white;
            border: none;
            padding: 15px 25px;
            border-radius: 10px;
            margin: 10px;
            font-size: 18px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h1>🎮 لودو ستار</h1>
    <p>لعبة لودو تفاعلية داخل التلجرام</p>
    
    <button class="btn" onclick="joinGame()">🎲 انضم للعبة</button>
    <button class="btn" onclick="rollDice()">🎲 رمي النرد</button>
    
    <div id="status" style="margin-top: 20px;"></div>

    <script>
        // Telegram Web App
        const tg = window.Telegram.WebApp;
        tg.expand();
        tg.ready();

        function joinGame() {
            tg.sendData(JSON.stringify({
                action: 'join_game'
            }));
            document.getElementById('status').innerText = '✅ انضممت للعبة!';
        }

        function rollDice() {
            const dice = Math.floor(Math.random() * 6) + 1;
            tg.sendData(JSON.stringify({
                action: 'roll_dice',
                dice: dice
            }));
            document.getElementById('status').innerText = `🎲 رميت النرد: ${dice}`;
        }

        // زر التحديث
        tg.MainButton.setText("🔄 تحديث").show();
        tg.MainButton.onClick(() => location.reload());
    </script>
</body>
</html>
